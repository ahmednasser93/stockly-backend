{
	"name": "stockly-api",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"deploy:prod": "bash scripts/deploy-prod.sh",
		"build": "wrangler deploy --dry-run",
		"dev": "npm run db:migrate:local && wrangler dev --local --persist-to .wrangler/state",
		"start": "wrangler dev --local --persist-to .wrangler/state",
		"test": "vitest run",
		"test:watch": "vitest watch",
		"test:ui:coverage": "npx vitest --ui --coverage",
		"test:coverage": "vitest run --coverage",
		"test:integration": "vitest run test/integration",
		"test:performance:load": "k6 run test/performance/load-test.js",
		"test:performance:stress": "k6 run test/performance/stress-test.js",
		"test:performance:spike": "k6 run test/performance/spike-test.js",
		"test:performance:endurance": "k6 run test/performance/endurance-test.js",
		"doc": "node scripts/doc-server.mjs",
		"cf-typegen": "wrangler types",
		"db:migrate:local": "wrangler d1 migrations apply stockly --local --persist-to .wrangler/state",
		"db:migrate:production": "wrangler d1 migrations apply stockly --remote",
		"select-prices": "wrangler d1 execute stockly --local --command=\"SELECT * FROM stock_prices ORDER BY id DESC LIMIT 20;\"",
		"select-search": "wrangler d1 execute stockly --local --command=\"SELECT * FROM search_cache ORDER BY id DESC;\"",
		"prod:select-prices": "wrangler d1 execute stockly --remote --command=\"SELECT * FROM stock_prices ORDER BY id DESC LIMIT 20;\"",
		"prod:select-search": "wrangler d1 execute stockly --remote --command=\"SELECT * FROM search_cache ORDER BY id DESC;\"",
		"prod:select-users": "wrangler d1 execute stockly --remote --command=\"SELECT id, email, username, name, created_at FROM users ORDER BY created_at DESC;\""
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"@vitest/coverage-istanbul": "~3.2.0",
		"@vitest/coverage-v8": "^3.2.4",
		"@vitest/ui": "^3.2.4",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.48.0"
	},
	"dependencies": {
		"jose": "^6.1.3"
	}
}